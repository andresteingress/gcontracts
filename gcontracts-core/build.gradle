project(":gcontracts-core") {
  dependencies {
    compile asm
    testCompile junit
    testRuntime junit
  }
}

test {
  // exclude basic test case classes
  excludes = ['org/gcontracts/tests/basic/**']
}
